# Dependências principais do pipeline.
# Instale com: python -m pip install -r requirements.txt
numpy>=1.26
pandas>=2.1
scipy>=1.11
scikit-learn>=1.4
xgboost>=2.0
shap>=0.45
numba>=0.61,<1.0
llvmlite>=0.44,<1.0
matplotlib>=3.8
PyYAML>=6.0
joblib>=1.3

# Interface unificada opcional para Google AI, OpenAI, Anthropic e outros LLMs.
# O simulador local não realiza chamadas de API.
litellm>=1.0,<2.0

tabulate>=0.9  # necessário para pandas.DataFrame.to_markdown() no relatório automático

# Interface de demonstração somente leitura para a banca.
streamlit>=1.45,<2.0
